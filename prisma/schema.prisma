generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Test {
  id         String   @id @default(uuid())
  ipAddress  String
  aiPrompt   String
  mbtiResult String
  scores     String   // JSON string stored as text (SQLite doesn't support JSONB)
  createdAt  DateTime @default(now())

  @@index([ipAddress, createdAt])
}
